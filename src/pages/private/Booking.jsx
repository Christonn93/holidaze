import { Alert } from "@mui/material";
import React from "react";

const Booking = () => {
 return (
  <>
   <Alert severity="warning">This function is not yet ready. It will come at a later stage.</Alert>
  </>
 );
};

export default Booking;
